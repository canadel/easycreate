#content
  #document-form-status
    Loading...
  #document-form{ :style => 'display: none' }
    = twitter_bootstrap_form_for 'document' do |f|
      = f.inputs 'Edit my document' do
        = f.text_field :title
        = f.text_area :content

        = f.actions do
          = f.submit 'Update', :id => 'submit'

= render 'edit.js'

